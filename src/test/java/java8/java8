hashmap底层的加入红黑树，concurrenthashmap将原来的锁分段变为cas无锁技术
lambda：java8/test/T1_Lambda
lambda需要函数式接口的支持：接口中只有一个抽象方法 -->注解：@FunctionalInterface
java内置4大核心函数式接口：java8/test/T2_FourInterface
    还有更细节的子接口

方法引用，构造器引用

Stream流：[4种创建方式]
    集合讲的是存储，流讲的是计算
    流不会自己存储元素
    流不会改变源对象，会返回一个持有新结果的流
    流的操作是延迟的，会等到需要结果的时候才执行



//接口支持方法体  得用default修饰  接口默认方法的类优先原则

//java8时间API   传统的时间api都存在线程安全问题