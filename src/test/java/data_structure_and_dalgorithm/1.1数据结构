----------------- 存储结构 -----------------
顺序结构
    存储的时候排序
    存储的时候不排序
链式结构
----------------- 逻辑结构 -----------------
1.集合结构【List线性表】【元素同属于一个集合】

2.线性结构【元素一对一的关系】
    树组，栈，队列，链表，哈希表。。。
3.树结构【元素一对多】

    二叉树，二叉搜索树，AVL，红黑树，Treap，Splay，堆，Trie，线段树，K-D树，并查树，哈夫曼树。。。
4.图结构【多对多】
    元素叫顶点  连线叫边  如果两顶点只通过一个边连接 就叫邻接顶点
    路径：路过的顶点
    有向图 无向图
    带权图 边有值代表某种意义


    邻接矩阵，邻接表

-------------------------------------------
解决散列冲突/hashmap hash碰撞
    开放定址法（线性探测再散列，二次探测再散列，伪随机探测再散列）
    再哈希法
    链地址法
    建立一个公共溢出区

----------------- 物理结构 -----------------
这里实际研究的是：如何数据元素存储到计算机的存储器中【存储器针对内存而言】   硬盘，软盘，光盘【常用文件存储结构来描述】
数据元素的存储形式有两种：
    顺序存储：元素是挨着的：元素的物理关系与逻辑关系一致【eg:树组】
    链式存储：不连续可以存储在任意的存储单元里【数据存储关系不能反应物理关系（通过指针来存放数据元素的地址）】【每个元素都多耗费了一点空间cause:数据+指针】